{
  "mcpServers": {
    "sage": {
      "command": "${CLAUDE_PLUGIN_ROOT}/server/run-sage.sh",
      "args": [],
      "env": {
        "SAGE_HOME": "${CLAUDE_WORKSPACE}/.sage"
      },
      "description": "Semantic memory - checkpoints, knowledge, code context"
    }
  }
}
